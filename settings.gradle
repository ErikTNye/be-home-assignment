plugins {
	// Lets Gradle auto-provision the Java 17 toolchain on machines that don't
	// have a JDK 17 installed
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'be-home-assignment'
